/*
 * Copyright (c) 2025 Renesas Electronics Corporation
 *
 * SPDX-License-Identifier: Apache-2.0
 */

/delete-node/ &qspi;
/delete-node/ &uart1;

/* Disable NFC so P0.09 and P0.10 can be used as GPIOs */
&nfct {
    status = "disabled";
};

/ {
    chosen {
        /delete-property/ zephyr,wifi;
	};

    aliases {
        wakeup-gpio = &gpio_wakeup;
    };

    gpio_wakeup {
        compatible = "gpio-leds";
        gpio_wakeup: wakeup_gpio {
           gpios = <&gpio0 10 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>;
            label = "Wakeup GPIO P0.10";
        };
    };
};

&pinctrl {
    spi4_default: spi4_default {
        group1 {
            psels = <NRF_PSEL(SPIM_SCK, 1, 15)>,
                    <NRF_PSEL(SPIM_MISO, 1, 14)>,
                    <NRF_PSEL(SPIM_MOSI, 1, 13)>;
       
        };
    };
    spi4_sleep: spi4_sleep {
        group1 {
            psels = <NRF_PSEL(SPIM_SCK, 1, 15)>,
                    <NRF_PSEL(SPIM_MISO, 1, 14)>,
                    <NRF_PSEL(SPIM_MOSI, 1, 13)>;
           
            low-power-enable;
        };
    };
};

&spi4 {
    compatible = "nordic,nrf-spim";
    status = "okay";
    cs-gpios = <&gpio1 12 (GPIO_ACTIVE_LOW)>;
 
    pinctrl-0 = <&spi4_default>;
    pinctrl-1 = <&spi4_sleep>;
    pinctrl-names = "default", "sleep";
 
    ra6w1_erpc_wifi: ra6w1_erpc_wifi@0 {
        compatible = "renesas,erpc-wifi-spi";
        reg = <0>;
        label = "RA6W1";
        spi-max-frequency = <200000>;
        boot-duration-ms = <2000>;
        int-gpios = <&gpio0 24 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>;
        reset-gpios = <&gpio1 11 GPIO_ACTIVE_HIGH>;
        status = "okay";
    };
};
